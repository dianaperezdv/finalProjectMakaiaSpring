package com.example.finalProject.Services;

import org.junit.jupiter.api.Test;

public class Tests {
    //Create a test for the method "crearCliente" in ClienteService.java
    //The test should verify that a client is created correctly
    //The test should verify that a client is not created when the cedula is invalid
    //The test should verify that a client is not created when the cedula already exists
    //The test should verify that a client is not created when the cedula is invalid and already exists
    //The test should verify that a client is not created when the cedula is valid and does not exist
    @Test
    void crearCliente() {


    }
}
